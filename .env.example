# Loaded by request and --live. Only --live makes API calls. Never commit your real key.
TYPESAFE_API_KEY=
# Pin the documented version for repeatability; jev-latest is also supported.
JEV_MODEL=jev-1.13.0
